# loneliness — PICO-8 cart

## Project memory

At the start of every session, read `.claude/devlog.md` for the registered history of completed features, implementation notes, and tunables. This is the project's living changelog — it preserves context between sessions.

When a feature is complete, use the `devlog` skill to prompt the user and register it (append to `.claude/devlog.md`).
